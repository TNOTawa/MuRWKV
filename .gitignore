# secrets
.env
*.env
**/credentials*
**/*token*
# exception: postmortem analysis script (contains no secrets; tokenizer.py
# stays tracked the same way - already-tracked files ignore ignore rules)
!scripts/r2_postmortem/token_probe.py
**/*.pem
**/*.key

# data (large, never commit) — root-anchored so the src/murwkv/data/ package is tracked
/data/
*.tar.gz
*.flac
*.wav
*.mp3
*.mid.bin
babyslakh*/

# checkpoints / outputs
checkpoints/
hf_cache/
wandb/
*.pt
*.pth
*.ckpt
*.safetensors
*.npz

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
build/
dist/
*.egg-info/

# misc
.nfs*
nohup.out
core.*